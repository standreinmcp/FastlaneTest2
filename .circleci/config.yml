version: 2.1

jobs:
  build-and-test:
    macos:
      xcode: 11.3.0
    steps:
      - checkout
      - run: cd ios &&  pod install
      - run: cd ios && fastlane scan
      
workflows:
    build-and-test:
      jobs:
        - build-and-test